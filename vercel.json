{
  "buildCommand": "cd control_tower_app/frontend && npm install && npm run build",
  "outputDirectory": "control_tower_app/frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
